
	项目整体思路:
1. 两层控制流：第一层控制流处理接口并返回需要的数据；第二层控制流，对接口数据和WMS数据进行处理，ETL后续工作（实施细节未开始）
2. 第一层控制流：
(1) 在序列容器中 添加多个“执行进程任务”【实现接口python/.exe脚本,数据采用全删全导的方式】，接口返回完整数据，存入相应的Table中。（不设置执行顺序；可单独运行某个Task；每个EPT返回的数据均存储在一个DB（DBSource），不同的Table中；）；
(2) 脚本封装成 .exe，入口为根据每个接口的Param提供参数；
(3) 接口访问采取post方式;
(4) 存储过程: 将Json文件转化存储在SQL Server.....第一层的数据全部选择字符串型,原样存储进去【在第二层进行字符串格式的选择】
3. 第二层控制流：
(1) 创建数据源和数据源视图；将需要的数据放在数据源视图中；


	工作进展(针对2个接口):
1. 已完成接口访问和测试和封装;
2. 完成json存储SQLServer的部分coding

	问题:
1.  英联问题

	下一步工作:
1. 增量抽取数据（时间戳方式）测试;
2. SSIS的包自动化，在SQL上操作;
2. 数据mapping文档，待确认;
2. 数据流设计和任务调度设计；
3. 更新SQL2012和VS版本；
4. 完成所有接口的访问coding；
5. 接口有数据，将数据解析存储到SQL；
6. 第二层控制流：创建数据源和数据视图，将需要的数据存储在视图中；

	关于OMS接口文档的一些疑问:


	关于英联数据的疑问:
1. 需要访问哪些接口，每个接口返回的字段需要哪些？---数据mapping
2. 存入数据，进行接口返回数据存储测试;


